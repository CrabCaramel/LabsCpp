struct A
{
int f1;
int f2;
};

При определении структуры A создаётся тип, который занимает два поля типа int. Обычно int занимает 4 байта, поэтому вся структура занимает 8 байт. Поля расположены подряд: сначала f1, затем f2.

В функции main создаётся объект:

A a{ 1, 2 };

При этом выделяются 8 байт памяти на стеке. В первые 4 байта записывается значение 1 (это f1), в следующие 4 байта записывается значение 2 (это f2).

std::cout << a.f1 << std::endl;

Эта строка читает первые 4 байта структуры a и выводит значение 1.

std::cout << a.f2 << std::endl;

Эта строка читает следующие 4 байта и выводит значение 2.

Память объекта выглядит так:

offset 0: значение 1 → поле f1
offset 4: значение 2 → поле f2

Итоговый вывод программы:

1
2
