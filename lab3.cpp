ТИПЫ  (Types)
Это всё, что создаёт новый тип данных.

Leg — структура (тип)

Arm — структура (тип)

Person — структура (тип)

int — встроенный тип

std::array<Leg, 2> — тип (массив из 2 объектов Leg)

Arm[2] — тип (массив из 2 объектов Arm)

ОБЪЕКТЫ (Objects)

Это всё, что реально создаёт экземпляр типа (переменную).

В структурах:

Leg::length — объект типа int

Arm::power — объект типа int

Person::legs — объект типа std::array<Leg, 2>

внутри него 2 объекта типа Leg

Person::arms — объект типа Arm[2]

внутри него 2 объекта типа Arm

В main:

person — объект типа Person

содержит:

два Leg

два Arm

Итоговое перечисление объектов:
Внутри Person:

legs — объект (массив из 2 Leg)

arms — объект (массив из 2 Arm)

Внутри Leg:

length — объект int

Внутри Arm:

power — объект int

Внутри main():

person — объект Person
